---
phase: 08-animated-win-line
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/style.css
  - src/main.js
  - index.html
autonomous: false
requirements:
  - ANIM-02
  - ANIM-03

must_haves:
  truths:
    - "When a game ends in a win (human or computer), a line visibly draws itself through the three winning cells"
    - "All 8 win orientations display a correctly positioned and animated line (3 rows, 3 columns, 2 diagonals)"
    - "No win line appears when the game ends in a draw"
    - "Clicking New Game removes the win line — no residual line from the previous game"
    - "On a system with prefers-reduced-motion: reduce, the line appears instantly without animation"
    - "On small viewports the win line stays visually aligned with the winning cells"
  artifacts:
    - path: "src/style.css"
      provides: ".win-line base styles, 8 orientation classes, 4 @keyframes animations, prefers-reduced-motion guard"
      contains: "win-line--row0, win-line--diag-rl, win-draw, win-draw-col, win-draw-diag-lr, win-draw-diag-rl"
    - path: "src/main.js"
      provides: "WIN_LINE_CLASSES lookup table, showWinLine(), clearWinLine(), call sites in handleGameOver() and resetGame()"
      contains: "WIN_LINE_CLASSES, showWinLine, clearWinLine"
    - path: "index.html"
      provides: "#win-line DOM element as sibling of .board inside .board-wrapper"
      contains: "win-line"
  key_links:
    - from: "src/main.js:handleGameOver()"
      to: "winLineEl DOM element"
      via: "showWinLine(winPositions) — called when winPositions.length > 0"
      pattern: "showWinLine\\(win"
    - from: "src/main.js:resetGame()"
      to: "winLineEl DOM element"
      via: "clearWinLine() — hides element and resets className"
      pattern: "clearWinLine"
    - from: "src/style.css"
      to: "#win-line element"
      via: ".win-line + .win-line--* classes applied by showWinLine()"
      pattern: "win-line--"
---

<objective>
Verify that ANIM-02 (animated win line) is fully implemented and working correctly in the browser. Phase 3 pre-implemented this feature; this plan confirms all success criteria are satisfied via static analysis and manual browser testing.

Purpose: Phase 8 follows the verify-first pattern established in Phases 4 and 7 — no new code expected. Static checks confirm structural correctness; a manual checkpoint validates visual and interactive behaviour.

Output: Confirmed ANIM-02 + ANIM-03 satisfaction. Phase complete.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-animated-win-line/08-CONTEXT.md

<!-- Prior phase summary needed: Phase 7 established the verify-first pattern -->
@.planning/phases/07-sound-effects-mute/07-01-SUMMARY.md
</context>

<interfaces>
<!-- Key implementation already in place — no new code to write -->

From src/main.js (lines 44–69):
```javascript
const winLineEl = document.getElementById('win-line');

const WIN_LINE_CLASSES = {
  '0,1,2': 'win-line--row0',
  '3,4,5': 'win-line--row1',
  '6,7,8': 'win-line--row2',
  '0,3,6': 'win-line--col0',
  '1,4,7': 'win-line--col1',
  '2,5,8': 'win-line--col2',
  '0,4,8': 'win-line--diag-lr',
  '2,4,6': 'win-line--diag-rl',
};

function showWinLine(positions) {
  const key = [...positions].sort((a, b) => a - b).join(',');
  const cls = WIN_LINE_CLASSES[key];
  if (!cls) return;
  winLineEl.className = `win-line ${cls}`;
  winLineEl.hidden = false;
}

function clearWinLine() {
  winLineEl.hidden = true;
  winLineEl.className = 'win-line';
}
```

Call sites in src/main.js:
- Line 154: `if (winPositions.length) showWinLine(winPositions);` — inside handleGameOver()
- Line 266: `clearWinLine();` — inside resetGame()

From index.html (line 41):
```html
<div class="win-line" id="win-line" hidden aria-hidden="true"></div>
```

From src/style.css (lines 253–362):
- `.board-wrapper { position: relative; }` — positioning context for overlay
- `.win-line` — absolute overlay, 6px height, white (#fff 0.9 opacity), no pointer-events
- `@keyframes win-draw` — scaleX(0→1) for row lines
- `@keyframes win-draw-col` — rotate(90deg) + translateX(-50%) + scaleX(0→1) for columns
- `@keyframes win-draw-diag-lr` — rotate(45deg) + scaleX(0→1) for left-to-right diagonal
- `@keyframes win-draw-diag-rl` — rotate(135deg) + scaleX(0→1) for right-to-left diagonal
- Animation: 0.3s ease-out, 0.1s delay — scoped under `@media (prefers-reduced-motion: no-preference)`
- 8 position classes: `.win-line--row0/1/2`, `.win-line--col0/1/2`, `.win-line--diag-lr`, `.win-line--diag-rl`
- Diagonal anchor: `diag-rl` uses `left: 95%` + `rotate(135deg)` (Phase 4 bugfix already applied)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Static verification — confirm ANIM-02 and ANIM-03 structurally present</name>
  <files>src/style.css, src/main.js, index.html</files>
  <action>
Run the following grep checks (read-only) to confirm all structural requirements for ANIM-02 and ANIM-03 are in place. Each check must return a match — no code changes are expected or needed.

**JS checks (src/main.js):**
1. `WIN_LINE_CLASSES` lookup table exists with all 8 entries:
   ```
   grep -n "WIN_LINE_CLASSES" src/main.js
   ```
   Expected: at least 2 matches (declaration + usage in showWinLine)

2. All 8 win-pattern keys present in the lookup table:
   ```
   grep -c "'[0-9],[0-9],[0-9]'" src/main.js
   ```
   Expected: 8 (one per win combination)

3. `showWinLine` called in `handleGameOver` with winPositions:
   ```
   grep -n "showWinLine" src/main.js
   ```
   Expected: declaration (line ~58) + call site inside handleGameOver (line ~154)

4. `clearWinLine` called in `resetGame`:
   ```
   grep -n "clearWinLine" src/main.js
   ```
   Expected: declaration (line ~66) + call site inside resetGame (line ~266)

5. Draw path does NOT call showWinLine (guard `winPositions.length > 0` present):
   ```
   grep -n "winPositions.length" src/main.js
   ```
   Expected: match showing the length check guards the showWinLine call

**HTML check (index.html):**
6. `#win-line` element exists as sibling of `.board` inside `.board-wrapper`:
   ```
   grep -n "win-line" index.html
   ```
   Expected: `<div class="win-line" id="win-line" hidden aria-hidden="true"></div>`

**CSS checks (src/style.css):**
7. All 4 keyframe animations exist:
   ```
   grep -n "@keyframes win-draw" src/style.css
   ```
   Expected: 4 matches: win-draw, win-draw-col, win-draw-diag-lr, win-draw-diag-rl

8. All 8 win-line orientation classes exist:
   ```
   grep -c "win-line--" src/style.css
   ```
   Expected: ≥8 (8 position class definitions + usage in media queries)

9. prefers-reduced-motion guard scopes animations:
   ```
   grep -n "prefers-reduced-motion" src/style.css
   ```
   Expected: 2 matches — one scoping `.win-line` animation (base), one scoping column/diagonal overrides

10. Diagonal anchor uses Phase 4 bugfix (left: 95%, rotate(135deg) for diag-rl):
    ```
    grep -n "diag-rl" src/style.css
    ```
    Expected: `left: 95%` and `rotate(135deg)` in `.win-line--diag-rl`

**If ANY check fails:** Investigate the discrepancy. Document the mismatch in the checkpoint task below. Do NOT write new code — report to the user.

**If all 10 checks pass:** Proceed immediately to Task 2 (human checkpoint).
  </action>
  <verify>
    <automated>grep -n "WIN_LINE_CLASSES" src/main.js && grep -c "'[0-9],[0-9],[0-9]'" src/main.js && grep -n "showWinLine\|clearWinLine" src/main.js && grep -n "win-line" index.html && grep -n "@keyframes win-draw" src/style.css && grep -n "prefers-reduced-motion" src/style.css</automated>
  </verify>
  <done>All 10 static checks return matches. Zero failures. Output printed confirming each check.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual browser verification — all ANIM-02 success criteria</name>
  <files>src/style.css, src/main.js, index.html</files>
  <action>Human browser test: verify all ANIM-02 success criteria manually using the checklist in how-to-verify. No code changes expected — this is a read/interact-only verification step.</action>
  <verify>
    <automated>MISSING — human-only checkpoint; no automated command applies</automated>
  </verify>
  <done>User types "approved" confirming all 8+ browser scenarios pass: row/column/diagonal wins animate correctly, draw shows no line, new game clears line, small viewport alignment holds, prefers-reduced-motion suppresses animation.</done>
  <what-built>
Animated win line overlay — a white 6px bar that draws itself through winning cells over 0.3s (with 0.1s delay after the last piece appears). Pre-implemented in Phase 3; static checks in Task 1 confirmed structural presence. Eight orientation classes cover all win patterns; prefers-reduced-motion guard suppresses animation on accessible systems.
  </what-built>
  <how-to-verify>
Start the dev server if not already running:
```
npm run dev
```
Open http://localhost:5173 in your browser.

**Test 1 — Human win (row):**
1. Play X in positions 1, 2, 3 (top row) — let computer play wherever
2. ✅ After X fills the top row: a white line should animate left-to-right through cells 1–3
3. ✅ The line draws itself (scaleX) over ~0.3s — not a sudden appearance

**Test 2 — Human win (column):**
1. Start New Game. Play X in positions 1, 4, 7 (left column)
2. ✅ A vertical line should animate top-to-bottom through the left column

**Test 3 — Human win (diagonal):**
1. Start New Game. Play X in positions 1, 5, 9 (top-left to bottom-right diagonal)
2. ✅ A diagonal line should animate through those three cells (angled ~45°)

**Test 4 — Right-to-left diagonal:**
1. Start New Game. Play X in positions 3, 5, 7 (top-right to bottom-left diagonal)
2. ✅ A diagonal line should animate through those cells (angled ~135° from top-right)

**Test 5 — Computer win:**
1. Start New Game. Let the computer win (play randomly / cooperatively)
2. ✅ Win line appears for the computer's winning three cells too

**Test 6 — Draw (no win line):**
1. Start New Game. Fill the board without either player winning (9 moves, no three-in-a-row)
2. ✅ No win line should appear — the draw state shows no overlay

**Test 7 — New Game clears the line:**
1. Win a game (win line is visible)
2. Click "New Game"
3. ✅ Win line is immediately gone — no residual overlay on the new board

**Test 8 — Small viewport alignment:**
1. Open DevTools (F12) → toggle device toolbar → select iPhone SE (375×667) or narrow to ~375px wide
2. Win a game in any orientation (row, column, diagonal)
3. ✅ Win line visually aligns through the three winning cells — not offset or floating outside the board

**Test 9 — prefers-reduced-motion (optional but recommended):**
1. In DevTools → Rendering panel (⋮ → More tools → Rendering) → set "Emulate CSS media feature: prefers-reduced-motion" to "reduce"
2. Win a game
3. ✅ Win line appears instantly (no animation) — the line is there immediately when the game ends
4. ✅ Line is still correctly positioned (no missing line — just no animation)
5. Unset the emulation when done

All 9 tests (or 8 if skipping devtools-reduced-motion) should pass with no visual regressions.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues observed (which test, what happened vs expected)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JS → DOM | showWinLine() writes className to a controlled DOM element — no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | WIN_LINE_CLASSES lookup | accept | Keys are derived from sorted game positions (integers 0–8) produced by Rust WASM — not user-controlled strings. No injection surface. |
| T-08-02 | Denial of Service | winLineEl.className assignment | accept | Element reference is obtained once at module load; no dynamic element creation; pointer-events: none prevents click interception. Low-value target in a local game. |
</threat_model>

<verification>
All success criteria confirmed when:
1. All 10 static grep checks in Task 1 return matches with no failures
2. Human approves Task 2 checkpoint after testing all 8 scenarios (9 with devtools)
3. ANIM-02 and ANIM-03 marked complete in REQUIREMENTS.md
</verification>

<success_criteria>
- Static verification: 10/10 grep checks pass (WIN_LINE_CLASSES, showWinLine, clearWinLine, keyframes, orientation classes, prefers-reduced-motion guard, diagonal bugfix anchor)
- Browser verification: win line animates correctly for human win (row, column, both diagonals), computer win; no line on draw; line clears on New Game; aligned on small viewport
- prefers-reduced-motion: line appears instantly (no animation) under `reduce` setting — line is still present
- ANIM-02 and ANIM-03 requirements marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/08-animated-win-line/08-01-SUMMARY.md` using the summary template.
</output>
